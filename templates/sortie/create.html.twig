{% extends 'base.html.twig' %}

{% block title %}
Creer une sortie | {{ parent() }}
{% endblock %}

{% block body %}
    <div>
        {{ form_start(sortieForm) }}
        {{ form_widget(sortieForm) }}
        <button>Enregistrer</button>
        {{ form_end(sortieForm) }}
    </div>
{% endblock %}

