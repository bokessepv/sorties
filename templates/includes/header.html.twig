<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        {% if app.user %}
            <div class="nav-item ml-5">
                <a class="navbar-brand" href="{{ path('main_home') }}">Sortir.com</a>
            </div>
        {% else %}
            <div class="nav-item ml-5">
                <a href="{{ path('app_login') }}"> <div class="navbar-brand">Sortir.com</div></a>
            </div>
        {% endif %}

        {% if app.user %}
            <div style="justify-content: flex-end">
                {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Admin
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                            <li><a class="dropdown-item" href="#">Gestion des villes</a></li>
                            <li><a class="dropdown-item" href="#">Gestion des campus</a></li>
                        </ul>
                    </li>
                {% endif %}
                <ul class="nav justify-content-end">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('main_home') }}" title="Accueil">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('main_profil', {'id' : app.user.id}) }}" title="Mon profil">Mon profil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}" title="Logout">Se deconnecter</a>
                    </li>
                </ul>
            </div>
        {% endif %}
</nav>


